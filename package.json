{
  "name": "shortselling-eu",
  "version": "1.0.0",
  "description": "ShortSelling.eu - European short-selling positions tracker",
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-deps": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
